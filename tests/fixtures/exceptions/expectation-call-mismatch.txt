Double `BookRepository` received a call to `find(456)` that doesn't match any of its configured expectations. `expects()` requires every call to match one exactly.

Here's how it compares to the configured expectation for `find`:
  id:
    - 123
    + 456
