
Can't auto-instantiate `BookRepositoryInterface` to passthru. It's an interface — so there's no constructor to invoke. You may need to pass an existing instance instead. For example: `->passthru($existingInstance)`.