Test double `Book` got an unexpected call to `getAuthor()` — Strict mode requires every call configured. Add: $book->allows('getAuthor')->returns(...);

Note: this test double was returned automatically. You will need to configure it explicitly if you want it to behave differently.