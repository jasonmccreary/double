Double `Book` received an unexpected call to `getAuthor()`. Strict mode requires every call to be configured. For example: `$book->allows('getAuthor')->returns(...)`.

Note: this double was returned automatically. You will need to configure it explicitly if you want it to behave differently.