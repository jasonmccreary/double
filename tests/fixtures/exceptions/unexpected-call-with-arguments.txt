Double `BookRepository` received an unexpected call to `save(5, 'Alice')`. This double is in Strict mode, so you need to configure every call. For example: `$bookRepository->allows('save')->returns(...)`.