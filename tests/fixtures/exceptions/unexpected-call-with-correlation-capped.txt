Test double `BookRepository` received an unexpected call to `find(6)`. Strict mode requires every call to be configured.

The following calls to `find` were made during this test: `find(1)`, `find(2)`, and 3 more
