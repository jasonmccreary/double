2 expectations were not satisfied on double `BookRepository`:

    expected `save(any arguments)` to be called exactly 1 time, but it was never called
    expected `delete(6)` to be called exactly 1 time, but it was never called (other calls: `delete(1)`, `delete(2)`, and 2 more)